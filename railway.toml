[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "npm run start:prod"
healthcheckPath = "/ping"  # 🔴 استخدم /ping بدلاً من /health
healthcheckTimeout = 10
healthcheckInterval = 30

[variables]
NODE_ENV = "production"